#posts
	- @posts.each do |post|
		.post
			%h2.title= link_to post.title, post
			Priorização:
			=post.points
			%p.date
				Publicado à
				= time_ago_in_words(post.created_at)
				Por
				= post.user.email